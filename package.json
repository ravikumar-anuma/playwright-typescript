{
  "name": "playwright-ts-framework",
  "version": "1.0.0",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install:mcp": "npm install mcp-server",
    "start:mcp": "npx mcp-server"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^25.0.3",
    "mcp-server": "^0.0.9",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  }
}
